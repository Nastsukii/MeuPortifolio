// Google Fonts are now loaded via _document.tsx to avoid build-time fetch errors.
// This file is kept for compatibility if needed.

export const roboto = {
  className: 'font-roboto',
  style: { fontFamily: 'Roboto, sans-serif' },
};
